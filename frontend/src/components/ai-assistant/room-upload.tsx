"use client";

import React from "react";

export function RoomUpload({ onUpload, className }: { onUpload?: (url: string) => void; className?: string }) {
  return <div className={className}>Upload room (stub)</div>;
}

export default RoomUpload;
