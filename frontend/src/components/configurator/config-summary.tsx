"use client";

import React from "react";

export function ConfigSummary({ className }: { className?: string }) {
  return <div className={className}>Config summary (stub)</div>;
}

export default ConfigSummary;
