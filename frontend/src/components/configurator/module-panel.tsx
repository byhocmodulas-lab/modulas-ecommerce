"use client";

import React from "react";

export function ModulePanel({ className }: { className?: string }) {
  return <div className={className}>Module panel (stub)</div>;
}

export default ModulePanel;
